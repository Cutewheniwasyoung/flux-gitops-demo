apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluxcd-gitops-demo
spec:
  selector:
    matchLabels:
      app: fluxcd-gitops-demo
  template:
    metadata:
      labels:
        app: fluxcd-gitops-demo
    spec:
      imagePullSecrets:
        - name: acr-secret      
      containers:
      - name: fluxcd-gitops-demo
        imagePullPolicy: IfNotPresent
        image: registry.cn-hangzhou.aliyuncs.com/fluxcd-demo/flux-gitops-demo:148de0889b3f73371ccf466accf8574a3f355178
        command: ["sleep", "9999"]
