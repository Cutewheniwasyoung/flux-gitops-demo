name: build

on:
  push:
    branches:
      # 以tag分支作为镜像tag
      - 'main'
    tags:
      - 'v*'

env:
  ALIYUN_REGISTRY: "${{ secrets.ALIYUN_REGISTRY }}"
  ALIYUN_NAME_SPACE: "${{ secrets.ALIYUN_NAME_SPACE }}"
  ALIYUN_REGISTRY_USER: "${{ secrets.ALIYUN_REGISTRY_USER }}"
  ALIYUN_REGISTRY_PASSWORD: "${{ secrets.ALIYUN_REGISTRY_PASSWORD }}"


jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: set outputs
        id: vars
        run: |
          # echo "::set-output name=tag::$(git show-ref --tags | grep $(git rev-parse HEAD) | awk '{print $2}' | awk -F'/' '{print $3}')"
          echo "::set-output name=tag::$(git rev-parse HEAD)"

      - name: Set up qemu
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Aliyun Docker registry
        run: |
          docker login -u ${{ env.ALIYUN_REGISTRY_USER }} -p ${{ env.ALIYUN_REGISTRY_PASSWORD }} ${{ env.ALIYUN_REGISTRY }}
      - name: build and push image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ env.ALIYUN_REGISTRY }}/${{ env.ALIYUN_NAME_SPACE }}/${{ github.event.repository.name }}:${{ steps.vars.outputs.tag }}

            